/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com","media.licdn.com", "aceternity.com", "i.pinimg.com", "upload.wikimedia.org"],
    },
};

export default nextConfig;